# ==============================================================================
# RAG CHATBOT - ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================================================
# ⚠️  NEVER commit real credentials! This file contains PLACEHOLDERS ONLY.
# Copy to .env and fill with actual values from your services.
# Always add .env to .gitignore (already configured).

# ==============================================================================
# DEPLOYMENT MODE (Phase 8 Production)
# ==============================================================================
ENVIRONMENT=development          # development | staging | production
DEBUG=True                        # False in production
HOST=0.0.0.0
PORT=8000                         # Render.com will override to dynamic port

# ==============================================================================
# DATABASE - NEON POSTGRESQL (REQUIRED)
# ==============================================================================
# Setup: https://neon.tech
# Get your connection string from Neon dashboard
# Format: postgresql://user:password@host/database?sslmode=require
# Example: postgresql://neondb_owner:mypass@ep-xxxxx.us-east-1.aws.neon.tech/rag_chatbot?sslmode=require
DATABASE_URL=postgresql://user:password@host:5432/database_name

# ==============================================================================
# VECTOR STORAGE - QDRANT CLOUD (REQUIRED)
# ==============================================================================
# Setup: https://cloud.qdrant.io
# Cluster URL: https://your-cluster-id.qdrant.io:6333
# API Key: Generated from Qdrant console
QDRANT_URL=https://your-cluster-id.qdrant.io:6333
QDRANT_API_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
QDRANT_COLLECTION_NAME=rag_chatbot

# ==============================================================================
# LLM & EMBEDDINGS - OPENAI API (REQUIRED)
# ==============================================================================
# Setup: https://platform.openai.com/api-keys
# Get API key from: https://platform.openai.com/account/api-keys
# IMPORTANT: Set monthly budget limit in account settings!
OPENAI_API_KEY=sk-proj-...your_key_here...

# Model Configuration (production-tested, optimized for cost & quality)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small    # 1536-dim, $0.02/M tokens
OPENAI_LLM_MODEL=gpt-4o                          # Primary model (best quality)
OPENAI_LLM_FALLBACK_MODEL=gpt-3.5-turbo          # Fallback if primary fails

# Generation Parameters
OPENAI_TEMPERATURE=0.3                           # 0.0 (deterministic) to 1.0 (creative)
OPENAI_MAX_TOKENS=500                            # Max tokens per response

# ==============================================================================
# CHAT & RATE LIMITING
# ==============================================================================
RATE_LIMIT_QUERIES_PER_MINUTE=10                 # Per session
RATE_LIMIT_QUERIES_PER_DAY=1000                  # Per IP
SESSION_TIMEOUT_HOURS=24                         # Session expiration

# Token Limits (for selected-text mode)
MAX_SELECTED_TEXT_TOKENS=2000                    # Max tokens to process
MAX_SELECTED_TEXT_CHARACTERS=10000               # Max chars to process

# ==============================================================================
# SECURITY & API AUTHENTICATION
# ==============================================================================
# Generate SECRET_KEY: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-key-change-in-production
API_KEY=default-key                              # For /ingest and /query endpoints

# JWT Configuration
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# CORS - Allowed Origins (comma-separated)
# Development: http://localhost:3000,http://localhost:8000
# Production: https://mehreen676.github.io,https://rag-chatbot-api.onrender.com
ALLOWED_ORIGINS_STR=http://localhost:3000,https://example.com

# ==============================================================================
# OAUTH 2.0 AUTHENTICATION (Optional - Phase 5+)
# ==============================================================================
# GitHub OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# OAuth Redirect URI
OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback
# Production: https://mehreen676.github.io/rag-chatbot/auth/callback

# ==============================================================================
# LOGGING
# ==============================================================================
LOG_LEVEL=INFO                   # DEBUG | INFO | WARNING | ERROR | CRITICAL

# ==============================================================================
# MONITORING & ERROR TRACKING (Optional - Phase 8 WAVE 3)
# ==============================================================================
# Sentry Error Tracking: https://sentry.io
# SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/123456
# SENTRY_ENVIRONMENT=production

# Datadog (optional)
# DATADOG_API_KEY=your_datadog_key
# DATADOG_APP_KEY=your_datadog_app_key

# ==============================================================================
# DEPLOYMENT NOTES
# ==============================================================================
# For Render.com deployment (WAVE 1):
# 1. Set ENVIRONMENT=production
# 2. Set DEBUG=False
# 3. Update ALLOWED_ORIGINS_STR for production domain
# 4. All secrets must be set in Render dashboard (Settings → Environment)
# 5. PORT will be automatically set by Render (do not override)
